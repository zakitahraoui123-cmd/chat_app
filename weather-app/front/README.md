# Fresh Weather (Frontend)

Modern React UI for the weather app.

## Run

```bash
npm install
npm run dev
```

Optional API base override:

- Copy `.env.example` → `.env.local`
- Set `VITE_API_URL=http://localhost:5050`

